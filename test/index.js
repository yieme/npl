var should = require('chai').should(),
    npl = require('..')
;

/*describe('npl', function() {
  it('should say hello', function(done) {
    var test = npl()
    test.value.should.equal('Hello, world');
    done();
  });
});
*/
